# notify-bot outbound webhook (best-effort chart forwarding).
# Leave blank to disable; failures never affect the public Telegram alert.
NOTIFY_BOT_WEBHOOK_URL=
# Bearer token shared with notify-bot's /webhooks/fvg-chart endpoint.
NOTIFY_BOT_WEBHOOK_TOKEN=
